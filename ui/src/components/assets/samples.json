{
    "ControlFlow": {
        "name": "Control Flow",
        "snippets": [
            ["if", "if [[ $foo ]]; then\nfi"],
            ["else if", "elif [[ $foo ]]; then"],
            ["for loop", "for i in $foo; do\ndone"],
            ["range for loop", "for i in {1..5}; do; do\ndone"]
        ]
    },
    "Declaration": {
        "name": "Declaration",
        "snippets": [
            ["function", "foo() {\n}"],
            ["variable", "foo=\"bar\""],
            ["array", "foo=( 0 1 2 )"],
            ["dictionary", "declare -A foo"]
        ]
    },
    "Conditional": {
        "name": "Conditional",
        "snippets": [
            ["equal", "[[ $foo -eq $bar ]]"],
            ["not equal", "[[ $foo -ne $bar ]]"],
            ["less than", "[[ $foo -lt $bar ]]"],
            ["greater than", "[[ $foo -gt $bar ]]"],
            ["Regexp", "[[ $foo =~ $bar ]]"]
        ]
    },
    "Miscellaneous": {
        "name": "Miscellaneous",
        "snippets": [
            ["shell execution", "echo \"Hello, World!\""],
            ["calculation", "$((foo + 1))"],
            ["inspect command", "command -V foo"]
        ]
    }
}
